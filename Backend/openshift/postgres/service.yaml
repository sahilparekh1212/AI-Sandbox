apiVersion: v1
kind: Service
metadata:
  name: postgres
  namespace: ask-app
  labels:
    app: postgres
spec:
  selector:
    app: postgres
  ports:
    - name: postgres
      port: 5432
      targetPort: 5432
