apiVersion: v1
kind: Service
metadata:
  name: notification-service
  namespace: ai-sandbox
  labels:
    app: notification-service
spec:
  selector:
    app: notification-service
  ports:
    - name: http
      port: 8084
      targetPort: 8084
