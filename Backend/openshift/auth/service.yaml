apiVersion: v1
kind: Service
metadata:
  name: auth-service
  namespace: ai-sandbox
  labels:
    app: auth-service
spec:
  selector:
    app: auth-service
  ports:
    - name: http
      port: 8085
      targetPort: 8085
