apiVersion: route.openshift.io/v1
kind: Route
metadata:
  name: auth-service
  namespace: ai-sandbox
  labels:
    app: auth-service
spec:
  to:
    kind: Service
    name: auth-service
  port:
    targetPort: http
  tls:
    termination: edge
    insecureEdgeTerminationPolicy: Redirect
