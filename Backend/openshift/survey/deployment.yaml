apiVersion: apps/v1
kind: Deployment
metadata:
  name: survey-service
  namespace: ai-sandbox
  labels:
    app: survey-service
spec:
  replicas: 1
  selector:
    matchLabels:
      app: survey-service
  template:
    metadata:
      labels:
        app: survey-service
    spec:
      containers:
        - name: survey-service
          image: image-registry.openshift-image-registry.svc:5000/ai-sandbox/survey-service:latest
          ports:
            - containerPort: 8084
          envFrom:
            - configMapRef:
                name: survey-service-config
          resources:
            requests:
              memory: "256Mi"
              cpu: "200m"
            limits:
              memory: "512Mi"
              cpu: "500m"
          livenessProbe:
            httpGet:
              path: /actuator/health
              port: 8084
            initialDelaySeconds: 60
            periodSeconds: 30
            failureThreshold: 3
          readinessProbe:
            httpGet:
              path: /actuator/health
              port: 8084
            initialDelaySeconds: 30
            periodSeconds: 10
            failureThreshold: 3
