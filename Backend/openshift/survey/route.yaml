apiVersion: route.openshift.io/v1
kind: Route
metadata:
  name: survey-service
  namespace: ai-sandbox
  labels:
    app: survey-service
spec:
  to:
    kind: Service
    name: survey-service
  port:
    targetPort: http
  tls:
    termination: edge
    insecureEdgeTerminationPolicy: Redirect
